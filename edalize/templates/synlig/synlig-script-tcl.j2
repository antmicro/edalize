yosys -import
{{ plugins }}
{% if plugins %}
yosys -import
{% endif %}
source edalize_synlig_procs.tcl

verilog_defaults -push
verilog_defaults -add -defer

set_defines
set_incdirs
read_files
set_params

verilog_defaults -pop

synth $top

{{ extra_passes }}

write_blif $name.blif
write_json $name.json
write_edif {{ edif_opts }} $name.edif
